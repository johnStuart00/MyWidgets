import 'package:flutter/material.dart';

class Teatrlar extends StatelessWidget {
  const Teatrlar({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Teatrlar"),
    );
  }
}