import 'package:flutter/material.dart';

class Konsertler extends StatelessWidget {
  const Konsertler({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Konsertler'),
    );
  }
}