import 'package:flutter/material.dart';

class Kinoteatrlar extends StatelessWidget {
  const Kinoteatrlar({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Kinoteatrlar'),
    );
  }
}